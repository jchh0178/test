<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
"http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="com.itwillbs.mapper.ReviewMapper">

    <insert id="insertReview" parameterType="ReviewDTO">
        INSERT INTO review (member_id, movie_id, review_rating, review_content, review_select, review_date, review_like)
        VALUES (#{memberId}, #{movieId}, #{reviewRating}, #{reviewContent}, #{reviewSelect}, NOW(), 0)
    </insert>

    <update id="updateReview" parameterType="ReviewDTO">
        UPDATE review
        SET review_content = #{reviewContent}
        WHERE review_id = #{reviewId}
    </update>

    <delete id="deleteReview" parameterType="int">
        DELETE FROM review WHERE review_id = #{reviewId}
    </delete>

    <select id="getReviewById" parameterType="int" resultType="ReviewDTO">
        SELECT * FROM review WHERE review_id = #{reviewId}
    </select>

    <select id="getReviewsByMovieId" parameterType="int" resultType="ReviewDTO">
        SELECT * FROM review WHERE movie_id = #{movieId} ORDER BY review_date DESC
    </select>

</mapper>
